

int main(int argc, char* argv[]){

	//set up the option list
	//Deuterium::Options::OptionList opt(argc, argv);

	//set up the logging
	//Deuterium::Logging::RootLogger::GetInstance().SetRootOutputName("DeuteriumPortListener");

	//log everything to file

	
}